import { useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { apiPost } from '../utils/dashboardApi';

export default function SetupPassword(){const [params]=useSearchParams();const navigate=useNavigate();const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();if(password!==confirm)return setError('Passwords do not match.');setBusy(true);setError('');try{await apiPost('/api/auth/setup-password',{token:params.get('token'),password});navigate('/login',{replace:true});}catch(err){setError(err.message);}finally{setBusy(false);}};return <main className="setup-page"><form className="setup-card" onSubmit={submit}><span className="page-eyebrow">Clockwrk workspace</span><h1>Set your password</h1><p>Your invite is single-use and expires after 72 hours.</p>{error&&<div className="form-error">{error}</div>}<label className="form-field">Password<input className="dash-input" type="password" minLength="10" required value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="form-field">Confirm password<input className="dash-input" type="password" minLength="10" required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button className="btn btn-primary" disabled={busy}>{busy?'Saving...':'Set password'}</button></form></main>}
